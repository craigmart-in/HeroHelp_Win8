<UserControl
    x:Class="HeroHelper.Controls.PassiveUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HeroHelper.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Image Width="42" Height="42"
                VerticalAlignment="Center"
                Source="{Binding Skill.DisplayIcon}"
                Stretch="None"
                Margin="0,0,3,0"/>
            <!-- {Binding Skill.Name} -->
            <TextBlock Text="{Binding Skill.Name}" 
                    VerticalAlignment="Center"
                    TextWrapping="Wrap"
                    Margin="5,0,0,0" Padding="1" Width="110"
                    Foreground="#FFF3E6D0" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
